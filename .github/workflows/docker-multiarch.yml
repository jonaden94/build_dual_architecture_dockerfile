name: Build & Push Docker (native multi-arch)

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  workflow_dispatch:

env:
  IMAGE: jonathanhenrich94/adv-stat-inf

jobs:
  build:
    name: Build ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runs_on: ubuntu-latest
          - platform: linux/arm64
            runs_on: ubuntu-24.04-arm
    runs-on: ${{ matrix.runs_on }}

    steps:
      - uses: actions/checkout@v4

      - name: Compute safe platform tag
        shell: bash
        run: |
          # turn "linux/amd64" -> "linux-amd64"
          SAFE_PLATFORM="${{ matrix.platform }}"
          SAFE_PLATFORM="${SAFE_PLATFORM//\//-}"
          echo "SAFE_PLATFORM=${SAFE_PLATFORM}" >> "$GITHUB_ENV"

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build and push a per-arch image *by digest*
      - name: Build & push (by digest)
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: ${{ matrix.platform }}
          push: true
          # IMPORTANT: tags must not contain "/"
          tags: ${{ env.IMAGE }}:temp-${{ env.SAFE_PLATFORM }}
          outputs: type=image,name=${{ env.IMAGE }},push-by-digest=true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Export digest
        shell: bash
        run: |
          mkdir -p /tmp/digests
          echo "${{ steps.build.outputs.digest }}" > /tmp/digests/digest-${{ env.SAFE_PLATFORM }}

      - name: Upload digest artifact
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ env.SAFE_PLATFORM }}
          path: /tmp/digests/digest-${{ env.SAFE_PLATFORM }}
          if-no-files-found: error

  merge:
    name: Merge multi-arch manifest
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          pattern: digests-*
          merge-multiple: true
          path: /tmp/digests

      - name: Compute tags
        id: meta
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VER="${GITHUB_REF#refs/tags/v}"
            echo "TAGS=${{ env.IMAGE }}:${VER} ${{ env.IMAGE }}:latest" >> "$GITHUB_OUTPUT"
          else
            echo "TAGS=${{ env.IMAGE }}:latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Create and push manifest list
        shell: bash
        run: |
          AMD="$(cat /tmp/digests/digest-linux-amd64 | tr -d '\n')"
          ARM="$(cat /tmp/digests/digest-linux-arm64 | tr -d '\n')"

          for TAG in ${{ steps.meta.outputs.TAGS }}; do
            docker buildx imagetools create \
              -t "$TAG" \
              "${{ env.IMAGE }}@${AMD}" \
              "${{ env.IMAGE }}@${ARM}"
          done
